# Retrospect (moved)

This topic moved to the `fa` skill: see [`skills/fa/retrospect.md`](../fa/retrospect.md).
Invoke it via `Skill("fa")` (or `Skill("fa", "retrospect")`). Do not re-add procedure text
here — this stub exists only so published references to the old path keep resolving.
