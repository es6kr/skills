# FA Prune (moved)

This topic moved to the `fa` skill: see [`skills/fa/fa-prune.md`](../fa/fa-prune.md).
Invoke it via `Skill("fa", "fa-prune")`. Do not re-add procedure text here — this stub
exists only so published references to the old path keep resolving.
