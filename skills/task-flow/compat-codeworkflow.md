# Backward Compatibility Topic (`compat-codeworkflow.md`)

## Backward Compatibility Mapping

The legacy `/code-workflow` command is mapped to `/task-flow` to maintain seamless operation for all existing workflows and scripts:

- `/code-workflow` ➔ invokes `/task-flow`
- Steps and review gates are fully preserved.
