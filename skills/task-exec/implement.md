# Implement Topic (`implement.md`)

## Test-Driven Development (TDD) Implementation

1. **TDD Cycle**:
   - Write failing unit/integration tests (Red).
   - Write minimal implementation code to pass (Green).
   - Clean up code and structure (Refactor).
2. **Superpowers Integration**:
   - Delegates to `superpowers:test-driven-development` and `superpowers:executing-plans`.
